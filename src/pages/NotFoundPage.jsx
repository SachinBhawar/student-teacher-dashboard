import React from "react";

const NotFoundPage = () => {
    return (
        <div className="h-screen w-screen flex flex-row items-center justify-center">Page Not Found...</div>
    );
};

export default NotFoundPage;
